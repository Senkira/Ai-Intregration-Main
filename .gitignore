# Exclude cloned source code repositories (they have their own git)
ClonedRepos/

# Legacy paths (for backward compatibility with existing local clones)
02/

# Exclude ESTATEMENT_API source (has its own git repo)
ESTATEMENT_API/

# Archives (too large for git)
*.rar
*.zip
*.7z
*.tar.gz

# Temp / scratch files
temp_search.txt
note

# Common
.DS_Store
Thumbs.db
*.user
.vs/
bin/
obj/
